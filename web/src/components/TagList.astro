---
import { formatLabel, normalizeSlug } from "@lib/content";

interface Props {
  tags: string[];
  base?: string;
}

const { tags, base = "/notebook/tags" } = Astro.props;
---

<ul class="flex flex-wrap gap-1.5">
  {
    tags.map((tag) => (
      <li>
        <a class="tag-link" href={`${base}/${normalizeSlug(tag)}`}>
          #{formatLabel(tag)}
        </a>
      </li>
    ))
  }
</ul>
